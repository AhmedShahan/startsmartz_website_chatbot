services:
  - type: web
    name: provider-api-validator
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn API:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
